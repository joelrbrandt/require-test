require-test
============

To run:

1. `npm install -g bower serf`
2. `bower install`
3. `serf -o`
4. Browse to `test-unbuilt` and `test-built`

To build:

1. `npm install -g requirejs`
2. `./build.sh`
